-- 114. An Introduction to CHECK Constraint
/*
A CHECK constraint is kind of a constraint that allows you to specify if values in a column
must meet a specific requirement

The CHECK constraint uses a boolean expression to evaluate the values before they are inserted
or updated to column

If the values pass the check, PostgreSQL will insert or update these values to the column.
Otherwise PostgreSQL will reject the changes and issue a constraint error

*/







































